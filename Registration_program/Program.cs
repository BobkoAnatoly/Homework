using Registration_program.Models;
using Microsoft.EntityFrameworkCore;
using System;
using Registration_program.Controller;
using System.Threading.Tasks;

namespace Registration_program
{
    internal class Program
    {
        static void Main(string[] args)
        {
            //CompetitionController competitionController = new CompetitionController();
            //Competition competition = new Competition() { Kind = CompetitionKind.Benchpress, Name = "WRPF" };
            //Task.Factory.StartNew(() => competitionController.Add(competition));
            //competitionController.Show();
            //Member member = new Member() { FirstName = "Кирилл ", LastName = "Бовбель",Age = 19};
            //MemberController memberController = new MemberController();
            //Task.Factory.StartNew(()=> memberController.Register(member, 1));
        }
    }
}
